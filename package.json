{
  "name": "multi-agent-research-assistant",
  "version": "1.0.0",
  "description": "Multi-agent research assistant using LangGraph, Groq, and Tavily",
  "scripts": {
    "dev": "uvicorn server:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn server:app --host 0.0.0.0 --port 8000",
    "install:py": "pip install -r requirements.txt"
  }
}
